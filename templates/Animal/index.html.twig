{% extends 'base.html.twig' %}
{% block title %} {{ titulo }} {% endblock %}
{% block body %} 
    <h1>{{ titulo }}</h1>
    <a href="{{ path('animal_cadastrar') }}" class="btn btn-primary">Cadastrar Animal</a>
    <table class="table">
        <thead>
            <tr>
                <td>#</td>
                <td>Qtd Leite</td>
                <td>Qtd Ração</td>
                <td>Peso</td>
                <td>Ações</td>
            </tr>
        </thead>
        <tbody>
            {% for animal in animais %}
                <tr>
                    <td>{{ animal.id }}</td>
                    <td>{{ animal.qntdLeite }}</td>
                    <td>{{ animal.qntdRacao }}</td>
                    <td>{{ animal.peso }}</td>
                    <td>
                        <a href="#" class="btn btn-primary">Editar</a>
                        <a href="#" class="btn btn-primary">Excluir</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}